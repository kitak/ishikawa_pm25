<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "石川県のPM2.5の測定値" %></title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag "application", :media => "all" %>
  </head>
  <body>

    <div class="navbar navbar-fluid-top">
      <div class="navbar-inner">
        <div class="container-fluid">
          <a class="brand" href="#">石川県のPM2.5の測定値</a>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>

    <div class="container">
      <div class="row">
        <header class="span12">
          <p><a href="http://www.pref.ishikawa.jp/cgi-bin/taiki/top.pl">石川県の大気環境の状況</a>からデータを取得しています. 不具合や要望等は<a href="https://twitter.com/kitak">@kitak</a>まで</p>
        </header>
      </div>
      <hr />
      <div class="row">
        
        <div class="span3" style="margin-top: 100px;">
          <div class="well sidebar-nav">
            <form class="form-horizontal" style="margin-bottom: 0px;">
               <p>日付を選ぶ</p>
               <%= select_month Time.now, {:use_month_numbers => true},
                                          {:id => 'month', :class => 'span1'} %>
               <span>月</span>
               <%= select_day Time.now, {:use_month_numbers => true},
                                        {:id => 'day', :class => 'span1'} %>
               <span>日</span>
               <p style="margin-top: 20px;"><button id="show_graph" class="btn btn-primary" type="button">グラフを表示</button></p>
            </form>
          </div><!--/.well -->
        </div><!--/span-->
        <div class="span8">
          <%= bootstrap_flash %>
          <%= yield %>
        </div>
      </div><!--/row-->

    </div> <!-- /container -->

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>

  </body>
</html>
